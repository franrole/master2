from setuptools import setup

setup(name='coclust',
      version='0.1',
      description='coclust algo',
      url='http://github.com/',
      author='xxx',
      author_email='xxx@example.com',
      license='MIT',
      packages=['coclust'],
      install_requires=[
          'numpy',
      ],
      zip_safe=False)
